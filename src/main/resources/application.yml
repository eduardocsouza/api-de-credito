Spring:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
  datasource:
    url: jdbc:h2:mem:credit_db

  jpa:
    show-sql: true
    hibernate:
    properties:
      hibernate.format_sql: true
